{
  "name": "dply",
  "version": "0.1.3",
  "private": true,
  "description": "No auth. No config. Just deploy.",
  "type": "module",
  "scripts": {
    "build": "bun run build:cli && bun run build:site && bun run build:release",
    "build:cli": "bun build src/main.ts --target=node --outfile dist/dply && chmod +x dist/dply",
    "build:release": "mkdir -p dist/release && cp dist/dply dist/release/dply && chmod +x dist/release/dply",
    "build:site": "vp build && cp scripts/install.sh dist/site/install.sh && cp skills/dply/SKILL.md dist/site/SKILL.md",
    "check": "tsc --noEmit",
    "deploy:site": "bun run build && npx --yes wrangler@latest deploy",
    "dev": "vp dev --host 0.0.0.0",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint",
    "smoke:dry": "bun run smoke:dry:core && bun run smoke:dry:frameworks",
    "smoke:dry:core": "./dist/dply --dry-run fixtures/single-html/index.html && ./dist/dply --dry-run fixtures/static-site && ./dist/dply --dry-run fixtures/worker-js/index.js",
    "smoke:dry:frameworks": "./dist/dply --dry-run fixtures/vite-app && ./dist/dply --dry-run fixtures/vue-app && ./dist/dply --dry-run fixtures/svelte-app && ./dist/dply --dry-run fixtures/next-app",
    "start": "bun run src/main.ts"
  },
  "dependencies": {
    "@cloudflare/kumo": "^2.6.0",
    "@vitejs/plugin-react": "^5.1.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "typescript": "^6.0.3",
    "vite-plus": "^0.2.1",
    "wrangler": "^4.105.0"
  }
}
